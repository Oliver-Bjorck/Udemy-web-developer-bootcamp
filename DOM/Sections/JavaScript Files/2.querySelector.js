const javaAnchor = document.querySelector('a[title="Java"]') //querySelector is a faster way to select an element based on their id, class, tag, whatever really
//querySelector will only select the first element that matches the parameters

const allParagraphs = document.querySelectorAll('p') //querySelectorAll will create an HTMLCollection with all elements matching the parameters