const math = require('./4.1.Working-With-Module-Exports') //we can require files, like shown here, in order to use their components
console.log(math);
console.log(math.square(9)); //we can then call these components in this file, such as the square function