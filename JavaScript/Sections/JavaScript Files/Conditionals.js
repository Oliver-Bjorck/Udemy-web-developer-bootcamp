console.log("before conditional")
let random = Math.random()
if(random < 0.5) { //if statements allow you to create conditional code that runs based on the conditions set within the statement
    console.log("your number is less than 0.5") //if the if statement is found to be correct, the code inside will run
    console.log(random); //if it isn't correct, the code will not run
}
console.log("after conditional")