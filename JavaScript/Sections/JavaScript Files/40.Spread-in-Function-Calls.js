const nums = [13, 4, 5, 21, 3, 3, 1, 2, 7, 6, 4, 2, 53456]

Math.max(...nums) //the spread syntax allows for elements within an array to spread across a function call
//If we were to simply input the array, it would show NaN as it takes the array as a whole, rather than the elements within