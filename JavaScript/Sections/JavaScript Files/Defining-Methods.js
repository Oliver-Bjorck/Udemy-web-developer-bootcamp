const myMath = {
    PI: 3.14159,
    square: function (num) { //methods are simply a function that's been added as a property on an onject
        return num * num;
    },
    cube(num) { //there is a shorthand way of implementing methods as well
        return num ** 3;
    }
}