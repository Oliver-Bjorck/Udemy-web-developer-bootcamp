let radius = 8;
if (radius > 0) {  
    const PI = 3.14159; //variables can also be scoped to a block (i.e. conditionals, loops, anything contained within curly braces)
    var msg = 'HIII!'; //the var keyword can override this, however this is an outdated keyword
}

console.log(radius);
console.log(PI);